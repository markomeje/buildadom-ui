import React from 'react'

const Loader = () => {
  return (
    <div className={`w-full py-4 h-full flex items-center justify-center `}>
      <div className="loading-spinner"></div>
    </div>
  )
}

export default Loader
